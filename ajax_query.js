

jQuery(document).ready(function($){

	/*$("#passphrase").focusout( function() {

			// make sure there is something to validate
			if ( !$('#passphrase').val().length )
			{
				// inform user of error
				$('#passphrase_invalid').html('Enter a passphrase');
			} else {

				var data = {

					action: 'validPassphrase',
					passphrase: $('#passphrase').val()
				};
				$.post(cbWeddingAjax.ajaxurl, data, function(response){

					// passphrase validation result
					$('#passphrase_invalid').html(response);

					// 
				});
			}
	});*/

});